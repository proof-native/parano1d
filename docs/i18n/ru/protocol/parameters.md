# Параметры консенсуса — v2

Здесь описаны действующие правила v2, применяемые с **mainnet H210537**. До этой высоты действует [архивный профиль](../archive/legacy-profiles.md). Genesis mainnet: 2026-08-21 16:00:00 UTC (`1787328000`).

| Параметр | Значение v2 |
| --- | --- |
| Целевой интервал блока | 30 с |
| Эпоха ASERT / полупериод | 6 блоков / 180 с |
| Медиана времени / дрейф в будущее | 11 заголовков / 120 с |
| Эпоха транзакций | 144 блока ≈ 72 мин |
| Финальность / максимальная реорганизация | 18 / 17 блоков |
| Аутентифицированный недавний суффикс | 18 блоков ≈ 9 мин |
| Хранение тел / undo | 42 / 36 блоков ≈ 21 / 18 мин |
| Small | m23 / 63 страницы / 504 входа / 63 вызова |
| Large | m24 / 206 страниц / 504 входа / 63 вызова |
| ABI контрактов | 3 / 16 инструкций / 2 постоянных u64 / 2 временных u64 |
| Домен State / сегмент | 2^24…2^32 / 2^16 слотов |
| Расширение | ≥75% заполненности в 10 из 18 финализированных заголовков |
| Сегменты на блок | 256 |
| Физическая страница | 323 байта / 8 входов / 2 выхода |
| Обычная логическая транзакция | 1…128 страниц с учётом лимитов страниц и входов класса |
| Заголовок / предел декодирования блока | 212 / 82,905 байт |
| Дерево транзакций | 256 листьев, ID логических транзакций |
| Предел передачи терминала | 1,100,000 байт |
| Граница терминала Small / Large | 1,014,132 / 1,081,396 байт |
| Разовый предел ответа с происхождением форка | 48 МиБ |
| Пределы авторизации / обычного intent | 256 КиБ / 303,495 байт |
| Пределы платёжной / контрактной квитанции | 128 КиБ / 1,110,624 байт |
| Предел HTTP-запроса RPC | 2,237,632 байт |
| Мемпул: число / байты | 1,024 / 384 МиБ |
| P2P / локальный RPC | 9600 / 9601 |
| Срок внешнего шаблона | 30 с после подготовки |

Бюджеты страниц, входов и вызовов действуют одновременно. Вызов занимает одну
страницу и один вход. Small вмещает 63 одностраничных платежа или 63 вызова;
Large — 206 платежей или 63 вызова и 143 платежа при общем пределе 504 входа.
Основной coinbase выделен отдельно. Дополнительная обязательная системная
страница уменьшает доступное число пользовательских страниц до 62 или 205.

Создание Large разрешается серверным флагом `--v2-large-blocks`; все узлы
проверяют оба класса. В GUI переключателя нет. Флаг разрешает выбор Large по
комиссиям, а не принуждает строить каждый блок этим классом.

Физический кодек поддерживает до 256 тел, 1 020 входов и 256 выходов логической
транзакции там, где это требуется историческим данным. Допуск v2 дополнительно
ограничивает входы числом 504 и страницы бюджетом класса. Предел кодека не
означает доступную вместимость. Число блоков — правило консенсуса; пересчёт
в минуты использует целевой интервал и не является сроком исполнения.

Эмиссия: 16 → 11,30 → 8 → 5,65 → 4 → 2,83 → 2 → 1,41 → 1 NOID,
ступень каждые 1 051 200 блоков от активации. Точные суммы, комиссии и причины —
в [экономике сети](economics.md), ядро — в [контрактах](../contracts/index.md),
измеренные затраты — в [производительности](../reference/performance.md).
